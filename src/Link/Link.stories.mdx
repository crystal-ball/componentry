import { ArgsTable, Meta } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Link } from './Link'

<Meta
  title='Components/Link'
  component={Link}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Link

<ArgsTable of={Link} />

## Variant

A **primary** variant link:

<Link href='#test'>Componentry link</Link>

---

## Links without routing

A Link without an `href` or a `to` prop will render a `button` element:

<Link onClick={console.log}>Anchor</Link>
