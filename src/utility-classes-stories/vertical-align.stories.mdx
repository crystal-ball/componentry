import { Meta } from '@storybook/addon-docs/blocks'

import { alignments } from '../storybook/constants'
import { ExampleIcon } from './_story-components'
import ElementsProvider from '../storybook/ElementsProvider'

<Meta title='Utility classes|Vertical Align' />

<ElementsProvider>

## Vertical align

<div className='border-300 border-top border-left border-right rounded p-3 w-100'>
  <h4 className='vertical-align-guides'>
    {alignments.map(alignment => (
      <>
        {alignment}:
        <ExampleIcon className={`align-${alignment}`} />
      </>
    ))}
  </h4>
</div>

</ElementsProvider>
