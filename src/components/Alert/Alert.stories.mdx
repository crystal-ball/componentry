import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Active, Alert, Link, Text } from '..'

<Meta
  title='Components/Alert'
  component={Alert}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Alert

Alerts can contain any HTML elements, and can be dismissible or static. A dismissible
Alert requires Componentry Active `active` and `deactivate` props, which can be passed
directly or the Alert can be nested inside of an Active component to automatically create
an uncontrolled Alert.

<Canvas>
  <Story
    name='Colors'
    decorators={[
      (Story) => (
        <div className='grid gap-2'>
          <Story />
        </div>
      ),
    ]}
  >
    <Alert color='primary'>
      <Text>Primary themed alert</Text>
    </Alert>
    <Alert color='info'>Info themed alert</Alert>
    <Alert color='success'>Success themed alert</Alert>
    <Alert color='warning'>Warning themed alert</Alert>
    <Alert color='error'>Error themed alert</Alert>
  </Story>
</Canvas>

## Alert elements

Styles are also included for: `alert-heading`, `alert-link`, and `<hr />` elements.

<Alert color='primary'>
  <Text variant='h3' className='🅲Alert-heading'>
    Well done!
  </Text>
  <Text>You successfully read this important alert message.</Text>
  <hr className='my-2' />
  <Link href='#demo' className='🅲Alert-link'>
    Go home
  </Link>
</Alert>

## Dismissible

Pass `dismissible` to an Alert to show a close icon which will call the `deactivate` prop
on click.

<Alert color='success' dismissible>
  <Text variant='h3' className='🅲Alert-heading'>
    Success!
  </Text>
  <Text>You created a dismissible alert 👋</Text>
</Alert>

## Accessibility

### Aria title

Provide an aria title for alerts with better accessibility.

<Alert color='primary' ariaTitle='Alert provides context on setting the aria title'>
  Pass an `ariaTitle` to provide more context.
</Alert>
