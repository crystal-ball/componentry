import { ArgsTable, Meta, Story } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Grid } from './Grid'

export function padNum(num) {
  return num > 9 ? num : `0${num}`
}

<Meta
  title='Components/Grid'
  component={Grid}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Grid

<Story name='Grid'>
  <Grid gap={2} backgroundColor='primary-200' className='rounded grid-cols-2'>
    {[1, 2].map((key) => (
      <div
        key={key}
        className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
      >
        #{padNum(key)}
      </div>
    ))}
  </Grid>
</Story>

Grid is a layout component with shorthand properties `align`, and `justify` for common
alignment styles.

<ArgsTable of={Grid} />
