import { ArgsTable, Meta } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Block } from './Block'

<Meta
  title='Components/Block'
  component={Block}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Block

Block is a convenience component for creating `display: block` containers.

<ArgsTable of={Block} />

## Block elements

Block components create block level containers:

<div>
  <Block border>Block content</Block>
  <Block border>Block content</Block>
</div>

## Inline block elements

Pass A truthy `inline` prop to create an block element with `display: inline-block`:

<div>
  <Block inline border>
    Block content
  </Block>
  <Block inline border>
    Block content
  </Block>
</div>
