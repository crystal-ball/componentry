import { ArgsTable, Meta, Story } from '@storybook/addon-docs'

import { Block } from './Block'

export function padNum(num) {
  return num > 9 ? num : `0${num}`
}

<Meta title='Components/Block' component={Block} />

# Block

<Story name='Block'>
  <Block backgroundColor='primary-200' className='rounded'>
    {[1, 2, 3].map((key) => (
      <div
        key={key}
        className='bg-primary-600 text-inverse font-mono font-bold px-6 py-4 rounded'
      >
        #{padNum(key)}
      </div>
    ))}
  </Block>
</Story>

Block is a layout component that renders a div.

<ArgsTable of={Block} />
