import { Meta } from '@storybook/addon-docs'

import { Button } from '../Button/Button'
import { ComponentryProvider } from './Provider'
import { ProviderConsumer } from './storybook/ProviderConsumer'

<Meta title='Components/ComponentryProvider' component={ComponentryProvider} />

# Theme

<ComponentryProvider
  components={{
    Button: {
      color: 'success',
      size: 'lg',
    },
  }}
>
  <div>
    <Button>A large successful button!</Button>
    <ProviderConsumer />
  </div>
</ComponentryProvider>
