// @flow
import React from 'react'

export default () => (
  <div>
    <p>Component contract</p>
    <p>
      - `as` prop - passthrough of any Props - prop `color` for theming - active,
      activate, deactivate usage for state elements - Trigger and Content
      subcomponents - Passing active, activate, deactivate props to create
      controlled components - Component control levels: Auto, Observer and
      Controlled
    </p>
  </div>
)
