<div>
  {(() => console.log('hello'))()}
  <Button onClick={toggleModal}>Toggle Modal</Button>
  <Modal active={modalActive} onDeactivate={toggleModal}>
    <Modal.Header>
      <Modal.Title>This is the header</Modal.Title>
      <Button link onClick={toggleModal}>
        <svg className="icon close font" role="img" aria-label="close">
          <use href="#close" />
        </svg>
      </Button>
    </Modal.Header>
    <Modal.Body>This is a modal</Modal.Body>
    <Modal.Footer>
      <Button color="dark" outline onClick={toggleModal}>
        Close
      </Button>
      <Button color="primary">Save Changes</Button>
    </Modal.Footer>
  </Modal>
</div>
