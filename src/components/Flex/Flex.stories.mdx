import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Flex } from './Flex'

export function padNum(num) {
  return num > 9 ? num : `0${num}`
}

<Meta
  title='Components/Flex'
  component={Flex}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Flex

<Story name='Flex'>
  <Flex gap={2} backgroundColor='primary-200' className='rounded'>
    {[1, 2, 3].map((key) => (
      <div
        key={key}
        className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
      >
        #{padNum(key)}
      </div>
    ))}
  </Flex>
</Story>

Flex is a layout component with shorthand properties `align`, `direction`, `justify`, and
`wrap` for common flex styles.

## Inline containers

Create inline level flex containers using the `inline` prop:

<Flex inline gap={2} backgroundColor='primary-200' className='rounded'>
  {[1, 2].map((key) => (
    <div
      key={key}
      className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
    >
      #0{key}
    </div>
  ))}
</Flex>
<Flex inline gap={2} backgroundColor='primary-200' className='rounded'>
  {[1, 2].map((key) => (
    <div
      key={key}
      className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
    >
      #0{key}
    </div>
  ))}
</Flex>

## Flex direction

Override the default direction of `row` with the `direction` prop:

<Flex direction='row-reverse' gap={2} backgroundColor='primary-200' className='rounded'>
  {[1, 2, 3].map((key) => (
    <div
      key={key}
      className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
    >
      #0{key}
    </div>
  ))}
</Flex>

## Justify and align

Set the flex items justification and alignment with the `justify` and `align` props:

<Flex
  justify='center'
  align='center'
  gap={2}
  backgroundColor='primary-200'
  className='rounded'
>
  {[1, 2, 3].map((key) => (
    <div
      key={key}
      className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
    >
      #0{key}
    </div>
  ))}
</Flex>

## Flex wrap

Override the flex wrap behavior with the `wrap` props:

<Flex wrap='wrap-reverse' gap={2} backgroundColor='primary-200' className='rounded'>
  {Array(12)
    .fill(0)
    .map((_, key) => (
      // eslint-disable-next-line react/no-array-index-key
      <div
        key={key}
        className='bg-primary-600 text-inverse font-mono font-bold px-3 py-2 rounded'
      >
        #{padNum(key)}
      </div>
    ))}
</Flex>

## Props

<ArgsTable of={Flex} />
