import { Theme } from '../../theme/theme'

//                                     <FormGroup /> styles
// --------------------------------------------------------

export const formGroupStyles = (theme: Theme) => ({
  '.C9Y-FormGroup': {
    marginBottom: theme.spacing[4],
  },
})
