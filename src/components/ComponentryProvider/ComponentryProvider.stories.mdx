import { Meta } from '@storybook/addon-docs'
import { BADGE } from '@geometricpanda/storybook-addon-badges'

import { Button } from '../Button/Button'
import { ComponentryProvider } from './ComponentryProvider'
import { ProviderConsumer } from './storybook/ProviderConsumer'

<Meta
  title='Components/Theme'
  component={Theme}
  parameters={{ badges: [BADGE.EXPERIMENTAL] }}
/>

# Theme

<ComponentryProvider
  theme={{
    Button: {
      color: 'success',
      size: 'lg',
    },
  }}
>
  <div>
    <Button>A large successful button!</Button>
    <ProviderConsumer />
  </div>
</ComponentryProvider>
