import Block from '../../Block/Block'
import { colors } from '../../storybook-resources'

<Meta title='Utility classes|Typography' />

<DocsTitle title='Typography' experimental />

## Theme colors

<div>
  {colors.map(color => (
    <>
      <small className='small'>.font-color-{color}</small>
      <p className={`p font-color-${color}`} key={color}>
        Componentry font colors can provide contextual hints.
      </p>
    </>
  ))}
</div>

---

## Font sizes

<small className="small">.font-size-xl</small>
<p className='p font-size-xl'>Example extra large text content.</p>
<small className="small">.font-size-lg</small>
<p className='p font-size-lg'>Example large text content.</p>
<small className="small">.font-size-base</small>
<p className='p font-size-base'>Example base size text content.</p>
<small className="small">.font-size-sm</small>
<p className='p font-size-sm'>Example small text content.</p>
<small className="small">.font-size-xs</small>
<p className='p font-size-xs'>Example extra small text content.</p>

---

## Font weights

<small className="small">.font-weight-bolder</small>
<p className='p font-weight-bolder'>The boldest font weight style.</p>
<small className="small">.font-weight-bold</small>
<p className='p font-weight-bold'>Bold font weight style.</p>
<small className="small">.font-weight-normal</small>
<p className='p font-weight-normal'>Normal font weight style.</p>
<small className="small">.font-weight-light</small>
<p className='p font-weight-light'>Light font weight style.</p>
<small className="small">.font-weight-lighter</small>
<p className='p font-weight-lighter'>The lightest font weight style.</p>

---

## Text transforms

<small className="small">.text-lowercase</small>
<p className='p text-lowercase'>Lowercase text transform.</p>
<small className="small">.text-uppercase</small>
<p className='p text-uppercase'>Uppercase text transform.</p>
<small className="small">.text-capitalize</small>
<p className='p text-capitalize'>Capitalize text transform.</p>

---

### Italic

<small className="small">.font-style-italic</small>
<p className='p font-style-italic'>This text is italic.</p>

### Monospace

<small className="small">.text-capitalize</small>
<p className='p text-monospace'>This text uses the monospace font-family.</p>
