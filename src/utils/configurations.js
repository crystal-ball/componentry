/**
 * These configurations can be overriden using context for a persistent configuration
 * override, or with a passed property for a single instance override.
 * @TODO: Docs
 */

/**
 * Any component with a visibility transition will use this time interval
 * @type {number}
 * @default 300
 */
export const visibilityTransitionLength = 300;
