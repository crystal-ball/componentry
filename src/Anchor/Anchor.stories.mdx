import { boolean, select } from '@storybook/addon-knobs'
import { action } from '@storybook/addon-actions'

import Anchor from './Anchor'

<Meta title='Components/Anchor' component={Anchor}></Meta>

<DocsTitle title='Anchors'></DocsTitle>

## Varaints

A **primary** variant anchor:

<Anchor href='#'>Anchor</Anchor>

---

## Colors

<div className='d-flex flex-column'>
  {['primary', 'success', 'warning', 'error'].map((color) => (
    <Anchor key={color} color={color} href='#'>
      Achor element
    </Anchor>
  ))}
</div>

## Anchors styled as buttons

An Anchor that looks like a button:

<Anchor baseClass='btn' href='#'>
  Anchor
</Anchor>
