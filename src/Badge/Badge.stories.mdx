import { Meta } from '@storybook/addon-docs/blocks'
import { boolean, select } from '@storybook/addon-knobs'

import Badge from './Badge'
import { ElementsProvider, colors, sizes } from '../story-resources'

<Meta title='Components|Badge' component={Badge} />

# Badges

A primary badge:

<Badge color='primary'>Badge</Badge>

## Colors

<div className='d-flex justify-content-center'>
  {colors.map(color => (
    <div className='px-1' key={color}>
      <Badge color={color}>Badge</Badge>
    </div>
  ))}
</div>

## Rounded badges

Pass the `rounded` prop to make a badge rounded:

<Badge color='primary' rounded>
  Badge
</Badge>

## Anchor badges

<Badge as='a' color='primary' href='http://componentry.design'>
  Componentry
</Badge>
