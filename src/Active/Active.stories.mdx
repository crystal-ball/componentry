<!-- eslint-disable no-console -->

import { Meta } from '@storybook/addon-docs/blocks'
import { DocsTitle } from '../storybook/DocsTitle'
import { Active } from './Active'

<Meta title='Components/Active' />

<DocsTitle title='Active' experimental />

## Uncontrolled component

<Active
  onActivate={console.log}
  onActivated={console.log}
  onDeactivate={console.log}
  onDeactivated={console.log}
>
  <Active.Action>Toggle</Active.Action>
  <Active.Content>Active component content</Active.Content>
</Active>

## Controlled

<Active activate={console.log}>
  <Active.Action>Toggle</Active.Action>
  <Active.Content>Active component content</Active.Content>
</Active>

## Default active override

<Active Action='Toggle' Content='Active component content' defaultActive />

## Deactivate on external clicks

<Active Action='Toggle' Content='Active component content' clickEvents />

## Deactivate on esc

<Active Action='Toggle' Content='Active component content' escEvents />

## Handle mouse events

<Active Action='Toggle' Content='Active component content' mouseEvents />

## Shorthand subcomponents

<Active Action='Toggle' Content='Active component content' />

## Size and direction

<Active size='sm' direction='up'>
  <Active.Action>Toggle</Active.Action>
  <Active.Content>Active component content</Active.Content>
</Active>

## FaCC pattern

<Active>
  {({ active, activate, deactivate }) => (
    <>
      <div>{String(active)}</div>
      <button type='button' onClick={activate}>
        activate
      </button>
      <button type='button' onClick={deactivate}>
        deactivate
      </button>
    </>
  )}
</Active>
