import { Tooltip } from './Tooltip'

<Meta title='Components/Tooltip' component={Tooltip} />

<DocsTitle title='Tooltips' experimental />

<Tooltip
  onActivate={console.log}
  onActivated={console.log}
  onDeactivate={console.log}
  onDeactivated={console.log}
>
  <Tooltip.Trigger>Fun fact</Tooltip.Trigger>
  <Tooltip.Content>
    Only 8% of the world’s currency is physical money, the rest only exists on computers.
  </Tooltip.Content>
</Tooltip>
