import State from './State'
import Content from './Content'
import Triggers from './Triggers'

const Toggle = Triggers('Toggle')
const Activate = Triggers('Activate')
const Deactivate = Triggers('Deactivate')

export { State, Content, Toggle, Activate, Deactivate }
